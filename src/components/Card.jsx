import React from 'react';

const Card = ({coffee}) => {
    const {nutrition_info,ingredients,category,image,name}= coffee
    return (
        <div>
            
        </div>
    );
};

export default Card;